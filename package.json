{
  "name": "moja-potrosnja-struje-android",
  "version": "1.0.2",
  "type": "module",
  "private": true,
  "description": "Besplatna lokalna Android aplikacija za evidenciju potrošnje električne energije.",
  "license": "GPL-3.0-only",
  "scripts": {
    "test": "node web/test.mjs",
    "sync:web": "node scripts/sync-web.mjs",
    "cap:sync": "npm run sync:web && npx cap sync android",
    "build:debug": "npm run cap:sync && cd android && ./gradlew assembleDebug",
    "build:release": "npm run cap:sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@bwip-js/browser": "^4.11.2",
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor/filesystem": "^7.1.8",
    "@capacitor/share": "^7.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
